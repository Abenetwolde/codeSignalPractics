function ArrayReplace(/* inputArray, elemToReplace, substitutionElem */) {
  let channel={name:'get to teh point'}
  channel.content=channel
    console.log(JSON.stringify(channel))
    
        }
       ArrayReplace(/* [1, 2, 1],1,3 */)